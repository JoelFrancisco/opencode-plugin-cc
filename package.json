{
  "name": "opencode-plugin-cc",
  "version": "0.1.2",
  "private": true,
  "type": "module",
  "description": "Use opencode from Claude Code to review code or delegate tasks.",
  "license": "MIT",
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "scripts": {
    "build": "pnpm -r build",
    "build:clean": "pnpm -r build:clean",
    "lint": "pnpm -r lint",
    "fmt": "pnpm -r fmt",
    "fmt:check": "pnpm -r fmt:check",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "test:e2e": "pnpm -r test:e2e",
    "test:live": "pnpm -r test:live"
  }
}

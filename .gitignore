node_modules/
*.log
*.tsbuildinfo
**/.tsbuildinfo

# Local tooling drop-files
.codex
.claude/

# dist/ is committed so the plugin works without a build step at install time.
# Uncomment during dev if you don't want commits to include built output:
# **/dist/
